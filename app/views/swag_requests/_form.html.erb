<%= form_for(swag_request) do |f| %>
  <% if swag_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(swag_request.errors.count, "error") %> prohibited this swag_request from being saved:</h2>

      <ul>
      <% swag_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :quantity %>
    <%= f.number_field :quantity %>
  </div>

  <div class="field">
    <%= f.label :gender %>
    <%= f.text_field :gender %>
  </div>

  <div class="field">
    <%= f.label :size %>
    <%= f.text_field :size %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
